# 灵动岛 Claude v5.0

> 多会话 · 刘海直接审批权限 · 完成门控 · 结果摘要 · 原生玻璃质感

v4.0 做到了"走开即用"，v5.0 把它推到能真正托管多个并行 Claude Code 会话——人离开电脑，哪个会话要权限就在刘海上点一下，真正完成了才庆祝，回来一眼看到结果。同时展开态换上了 macOS 原生玻璃质感。

<!-- 截图占位：放到 record/ 下同名文件
  record/v5-glass.png        展开态玻璃质感（厚玻璃 / 多组件卡片）
  record/v5-permission.png   收起态橙色权限条：左绿勾允许、右红叉拒绝、中间工具+命令
  record/v5-sessions.png     展开态 Claude 卡片的多会话行列表 + ×N 角标
  record/v5-result.png       完成后展开态显示最后回复摘要 + 上下文百分比
-->

| 玻璃质感 | 刘海审批 | 多会话 | 结果摘要 |
|----------|----------|--------|----------|
| ![玻璃](record/v5-glass.png) | ![审批](record/v5-permission.png) | ![多会话](record/v5-sessions.png) | ![结果](record/v5-result.png) |

## 新特性

### 多会话支持
多个 Claude Code 终端并行时按会话独立追踪（每会话一个状态文件），收起态第二行显示 `×N` 角标，展开态切换为会话列表（项目名 + 状态 + 当前工具）。按"需授权 > 出错 > 运行中"优先级聚合显示最需要关注的会话，并通过进程存活检测自动清理已退出会话的孤儿状态。

### 刘海直接批准权限
Claude Code 请求工具权限时，收起态左右图标变成 ✓ / ✗ 按钮（绿勾允许、红叉拒绝），不用切回终端；展开态 Claude 卡片显示完整命令 + 允许 / **始终允许** / 拒绝大按钮（"始终允许"会把规则写进项目的 `settings.local.json`，等同终端里的 Always allow）。在终端先回答的话刘海卡片自动消失；App 没运行则自动回退到终端确认，零风险。选择题类工具（如 AskUserQuestion）不弹审批卡，交回终端用选项菜单处理。

### 完成不再误报
Stop 时若仍有后台任务 / 定时任务 / stop-hook 续跑，则不触发完成动画；被伪装成普通结束的 API 错误（限流、计费失败等）会被识别为出错状态而非完成。

### 完成看得到结果
任务交回时展示 Claude 最后一条回复的摘要，展开态直接看结果。Claude 卡片标题带上下文窗口占用百分比（>60% 变黄、>85% 变红，提醒该收尾或 `/compact`）；窗口大小可在右键菜单按 1M 计算。

### 贴顶探出提醒
贴顶模式下文字被物理刘海挡住时，状态变化会让收起条向下探出一行提醒（约 1.5 秒收回），权限请求期间持续探出。右键菜单可关闭。

### 原生玻璃质感
展开态用 macOS 暗色振动玻璃，背后桌面实时虚化透出，深色保证文字清晰。右键菜单「展开玻璃质感」可切换**厚玻璃 / 薄玻璃**两档。展开 / 收起是原生灵动岛那种连续弹性 morph 动画（带回弹、圆角连续过渡、内容模糊交叉淡化）。

## 安装

下载 `NotchClaude.dmg`，打开后将 `NotchClaudeApp.app` 拖入 `Applications`。首次启动会自动配置 / 升级 Claude Code hook（合并式写入，保留其它工具的 hook）。

> 需要 macOS 13.0+；玻璃质感与展开动画在 macOS 26 上最佳。刘海审批用 Claude Code 的 HTTP PermissionRequest hook（约 2.1.x+）。

## 已知边界

- 上下文窗口大小 hook 无法可靠探测，默认按 200K 估算；1M 订阅用户在右键菜单勾选「上下文按 1M 窗口计算」。
- 若同时装了其它也注册 PermissionRequest hook 的工具（如 Clawd on Desk），两边的审批提示会同时出现，任一边回答均生效。
