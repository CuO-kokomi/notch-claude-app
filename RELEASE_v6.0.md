# 灵动岛 Claude v6.0

> 会话管理 · 面板内搜索 · 多终端恢复 · 全组件语义色

v5.0 让刘海能托管多个并行会话，v6.0 把时间轴拉长——不止看"正在跑的"，所有历史 Claude Code 会话都能在刘海上浏览、搜索、一键恢复。刘海从状态面板升级为会话入口。

> 会话组件 / 管理模式 / 搜索 / 语义色等效果截图见仓库 [README](README.md)。

## 新特性

### 会话管理（ccsm 集成）
新增「会话」组件：最近会话带项目色圆点 + 相对时间，点击即在新终端窗口 `claude --resume` 恢复；点「全部 N 个会话」进入**会话管理模式**——所有历史会话（扫描 `~/.claude/projects`）按项目分组，显示标题 / 首条消息 / 分支 / 消息数 / 时间，组头可折叠。首扫结果落盘缓存（mtime 增量），重启秒开。

### 面板内直接搜索
会话管理模式带搜索框，这是面板第一次支持键盘输入：Spotlight 式成为键盘焦点但不激活 app、不打断你手头的工作，退出时键盘自动还给原来的 app。即时过滤标题 / 路径；勾选「全文搜索」可搜会话正文（装了 ripgrep 自动加速，没装走内置搜索）。Esc 或右上角 ✗ 退出。

### 跳过权限恢复
勾选「跳过权限」后 resume 附加 `--dangerously-skip-permissions`，勾选持久化，组件与管理页共用。

### 多终端支持
右键菜单「会话打开终端」选择 resume 在哪个终端打开：Terminal / Otty / iTerm2（AppleScript），Ghostty / kitty / Alacritty / WezTerm（CLI 直启）。只列已安装的，所选终端卸载后自动回退 Terminal。

### 项目色 + 全组件语义色
每个项目从路径哈希出一个稳定的粉彩强调色（会话行圆点 + 项目名、管理页分组头同色），多项目扫一眼即可区分。全部组件按"一个组件一个语义色、只点信号不点底色"统一美化：系统监控四图标、计时器运行态薄荷绿、日历今日列橙、天气多色符号、音乐播放键品牌色、Git 品牌橙、端口青、Docker 品牌蓝、番茄钟相位色、快捷启动图标各自着色，并为可点行加了 hover 高亮。

## 修复

### 终端先回答权限后刘海卡片不消失
新版 Claude Code 在终端回答后不再断开挂起的 PermissionRequest 连接，刘海便一直显示待审批。改为用自身状态数据兜底：请求所属会话的状态在请求之后被推进、且不再是「需授权」，即视为已在别处处理并自动撤卡。

### 系统监控三项读数不准
- **CPU**：原实现直接用开机以来的累计 tick 算占比，显示的其实是「开机至今平均占用」——一个几乎不动的常数（实测长期钉在 8.9%）。改为两次采样做差值，实测空载 15–21%（同期 `top` 15.8%）、四核加压跳至 ~51%。
- **内存**：原用 `active + wired + compressed`，`active` 既包含活跃的文件缓存又漏掉 inactive 的匿名页。改为对齐活动监视器的「已用内存」= App 内存 + 联动内存 + 已压缩，实测差约 0.5G。
- **网速**：① 开 VPN / 代理 TUN 时，隧道接口与物理网卡承载的是同一批字节，原实现两边都累加，速度约为实际的 2 倍——改为只统计物理网卡（隧道流量必然经过物理网卡，不会漏算）；② 网卡字节计数器是 32 位、每 4GB 归零，原实现遇到负差值钳零，千兆速率下约每 43 秒闪一次 `0K/s`——改为逐接口做差 + 补 2³² 回绕修正。实测下载 40MB 文件，测得 40.2MB（约 5% 协议开销）。

## 安装

下载 `NotchClaude.dmg`，打开后将 `NotchClaudeApp.app` 拖入 `Applications`。首次启动会自动配置 / 升级 Claude Code hook（合并式写入，保留其它工具的 hook）。

> 需要 macOS 13.0+。首次 resume 会弹一次「控制 Terminal（或所选终端）」的自动化授权。

## 已知边界

- 全文搜索在没装 ripgrep 的机器上走内置逐文件搜索，会话量很大时可能需要几秒（上限 20 秒，返回已扫到的结果）。
- iTerm2 / Ghostty / kitty / Alacritty / WezTerm 的恢复方言按各自官方接口实现，如有异常欢迎提 Issue。
- Warp 没有可运行命令的自动化接口，暂不支持作为 resume 终端。
